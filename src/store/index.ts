import { createStore } from 'vuex'
import appGlobal from "@/store/modules/appGlobal";

export default createStore({
  // state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    app: appGlobal
  }
})

