export namespace QrcodeApi {
    export interface QrcodeParams {
        VertyValue: string;
    }
    export interface QrcodeResult {
        Url: string;
        LeftTime: number;
        IsVerty: boolean;
    }
}

